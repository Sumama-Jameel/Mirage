Done (2026-08-05): MiMo V2.5 web provider (mimo-v2.5, mimo-v2.5-pro, mimo-v2-flash,
mimo-v2-pro, mimo-v2-omni) fully live including XML-fallback function tools,
streaming, thinking, web search, image upload, session continuation. flash/pro/omni
still return upstream "server busy" server-side.

Done (2026-08-05): Mistral Le Chat web provider live end-to-end. Rewired the
direct HTTP client to the stealth wreq client (clears the Cloudflare gate without
cf_clearance), added the tRPC message.newChat create-chat step plus
mode:"start"/"append" payloads, fixed the state_update json-wrapper unwrap,
per-messageId patch accumulation, array-index patch paths, and the
contentChunks text extraction. Verified live: non-stream + stream chat, thinking
(reasoning_content), web search, image/file upload (tRPC file.uploadFile + Azure
PUT), and session continuation via session_url.
